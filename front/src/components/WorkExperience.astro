---
interface Props {
  experience: {
    title: string;
    period: string;
    location: string;
    tasks: string[];
    technologies: string[];
    current: boolean;
  };
  index: number;
}

const { experience, index } = Astro.props;
const prefix = index === 0 ? 'exp.total' : 'exp.allianz';

const dotClasses = [
  'absolute w-3 h-3 rounded-full -left-[7px] top-2',
  experience.current
    ? 'bg-violet-500 shadow-[0_0_12px_rgba(139,92,246,0.8)] animate-pulse'
    : 'bg-zinc-700'
].join(' ');
---

<div class="relative pl-8 border-l border-zinc-800/50 group">
  <div class={dotClasses}></div>
  <div class="flex flex-wrap justify-between items-baseline mb-2">
    <h3 class="text-xl font-bold text-white group-hover:text-violet-300 transition-colors duration-300" data-i18n={`${prefix}.title`}>{experience.title}</h3>
    <span class="text-zinc-600 font-['JetBrains_Mono'] text-sm" data-i18n={`${prefix}.period`}>{experience.period}</span>
  </div>
  <p class="text-zinc-600 mb-4 italic text-sm" data-i18n={`${prefix}.location`}>{experience.location}</p>
  <ul class="text-zinc-400 text-base space-y-2 max-w-3xl leading-relaxed">
    {experience.tasks.map((task: string, i: number) => (
      <li class="flex gap-3">
        <span class="text-violet-500 mt-1.5 flex-shrink-0">▸</span>
        <span data-i18n={`${prefix}.task${i + 1}`}>{task}</span>
      </li>
    ))}
  </ul>
  <div class="mt-4 flex flex-wrap gap-2">
    {experience.technologies.map((t: string) => (
      <span class="text-[10px] border border-zinc-800 text-zinc-500 px-2.5 py-1 rounded-full uppercase font-bold tracking-widest hover:border-violet-500/30 hover:text-violet-400 transition-colors duration-200">{t}</span>
    ))}
  </div>
</div>
