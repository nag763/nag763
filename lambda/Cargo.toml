[package]
name = "nag763-lambda"
version = "0.1.0"
edition = "2021"

[dependencies]
aws-config = "1.6.1"
aws-sdk-sesv2 = "1.74.0"
lambda_http = "0.14.0"
serde = { version = "1.0.219", features = ["derive"] }
serde_json = "1.0.140"

tokio = { version = "1", features = ["macros"] }
